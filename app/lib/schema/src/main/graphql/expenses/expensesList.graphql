query ExpensesList($month: Int!, $year: Int!, $page: Int!, $limit: Int) {
    expensesList(month: $month, year: $year, page: $page, limit: $limit) {
        expenses {
            ...expenseFragment
        },
        pagination {
            ...paginationFragment
        }
    }
}