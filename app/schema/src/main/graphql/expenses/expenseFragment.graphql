fragment expenseFragment on ExpenseDetail {
    currency { ...currencyInfo }
    selectedCurrency { ...currencyInfo }
    description,
    usdValue,
    value,
    cursor,
    id,
    date,
    category {
        ...category
    }
}